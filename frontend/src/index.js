/*
 * Entry point for the React application. In a real deployment this would be
 * bundled with a tool like Vite or Webpack and served to the browser. Here
 * we simply import the App component so that tests can import it. If you
 * wish to run this frontend locally you can integrate it into your own
 * build process.
 */

import App from './App.jsx';

export default App;